---
- hosts: private
  user: soehlert
  become: true
  become_user: root
  roles:
    - groups
    - user
    - sources
    - sudoers

- hosts: private
  user: soehlert
  become: true
  become_user: root
  roles:
    - packages
    - git
    - firewalld

- hosts: localhost
  user: soehlert
  become: true
  become_user: root
  roles:
    - groups
    - sudoers
    - packages
    - firewalld
