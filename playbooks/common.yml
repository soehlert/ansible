---
- hosts: private
  user: soehlert
  become: true
  become_user: root
  roles:
    - groups
    - user
    - packages
    - git

- hosts: centos
  user: soehlert
  become: true
  become_user: root
  roles:
    - selinux

- hosts: private
  user: soehlert
  become: true
  become_user: root
  roles:
    - sudoers

- hosts: centos7
  user: soehlert
  become: true
  become_user: root
  roles:
    - firewalld

- hosts: debian
  user: deploy
  become: true
  become_user: root
  roles:
    - duo
    - sources
