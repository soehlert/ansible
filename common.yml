---
- hosts: centos
  user: vagrant
  sudo: true
  sudo_user: root
  roles:
    - user

- hosts: debian
  user: vagrant
  sudo: true
  sudo_user: root
  roles:
    - user
  tasks:
  - group:
      name: wheel
      state: present
