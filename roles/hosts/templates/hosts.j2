127.0.0.1 localhost
{{ ansible_default_ipv4.address }} {{ ansible_hostname }}.{{ pri_domain_name }} {{ ansible_hostname }}
# The following lines are desirable for IPv6 capable hosts
::1     localhost ip6-localhost ip6-loopback
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters
{% if inventory_hostname in groups['dns'] %}
{% for item in dns_entries %}
{{ item.address }} {{ item.name }}.{{ pri_domain_name }} {{ item.name }}
{% endfor %}
{% endif %}
